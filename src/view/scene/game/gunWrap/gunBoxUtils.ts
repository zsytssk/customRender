export function getItemSkin() {
    const result = {} as {
        [key: string]: {
            skin?: string;
            visible: boolean;
        };
    };
    const ani_list = ['base', 'light', 'body'];
}
